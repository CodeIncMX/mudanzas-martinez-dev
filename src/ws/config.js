const configService = {
  apiUrl: 'http://test.mudanzas-martinez.com/api',
  headers: {
    'Content-Type': 'application/json', // Default header for methods with body (patch, post & put)
  },
};

export default configService;
